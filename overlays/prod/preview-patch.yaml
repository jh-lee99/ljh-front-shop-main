apiVersion: v1
kind: Service
metadata:
  name: ljh-front-shop-main-preview
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
  selector:
    app: ljh-front-shop-main
